# US-V11-02.2 : Détection connectivité

| Métadonnée | Valeur |
|------------|--------|
| **ID** | US-V11-02.2 |
| **Epic** | E-V11-02 : Offline Mode |
| **Milestone** | V1.1 |
| **Priorité** | P0 |
| **PRD** | [prd-faatere-v1.md](../../prd-faatere-v1.md) |

---

## User Story

**En tant que** app mobile,
**Je veux** détecter l'état de connexion,
**Afin de** basculer entre modes online/offline.

---

## Critères d'acceptance

- [ ] Listener NetInfo
- [ ] Indicateur visuel mode offline
- [ ] Notification à la reconnexion

---

## Indicateurs visuels

### Mode online

```
┌─────────────────────────────┐
│ Adhérents         [🔍] [+]  │  ← Pas d'indicateur spécial
├─────────────────────────────┤
```

### Mode offline

```
┌─────────────────────────────┐
│ 📴 Mode hors-ligne          │  ← Bannière orange
├─────────────────────────────┤
│ Adhérents         [🔍] [+]  │
├─────────────────────────────┤
```

### Reconnexion détectée

```
┌─────────────────────────────┐
│ ✓ Connexion rétablie        │  ← Bannière verte temporaire
│   Synchronisation...        │
├─────────────────────────────┤
```

---

## Tâches techniques

- [ ] Installer @react-native-community/netinfo
- [ ] Créer hook useConnectivity
- [ ] Créer context ConnectivityProvider
- [ ] Créer composant OfflineBanner
- [ ] Déclencher sync à la reconnexion
- [ ] Afficher notification de reconnexion

---

## Code exemple

```typescript
import NetInfo from '@react-native-community/netinfo';

const useConnectivity = () => {
  const [isConnected, setIsConnected] = useState(true);

  useEffect(() => {
    const unsubscribe = NetInfo.addEventListener(state => {
      setIsConnected(state.isConnected ?? false);
    });
    return unsubscribe;
  }, []);

  return { isConnected };
};
```

---

## Dépendances

- US-V11-02.1 : Stockage local

---

## Notes

La détection de connectivité doit être robuste pour éviter les faux positifs/négatifs.
