{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Frontend not yet configured'",
    "build": "echo 'Frontend not yet configured'",
    "lint": "echo 'Frontend not yet configured'",
    "test": "echo 'Frontend not yet configured'",
    "compile": "echo 'Frontend not yet configured'"
  }
}
